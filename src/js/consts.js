const DEFAULT_FOLDER_NAME = 'ImageShrink';

const DEST = 'destination';
const WARNING = 'warning';
const ERROR = 'error';

module.exports = {
    DEST,
    WARNING,
    ERROR,
    DEFAULT_FOLDER_NAME
}